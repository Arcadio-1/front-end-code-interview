import Title from "@/components/ui/Title";
import React from "react";
import { Container } from "./container";

const page = () => {
  return (
    <div>
      <Title>custom hook async with localStorage</Title>
      <Container />
    </div>
  );
};

export default page;
